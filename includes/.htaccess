# Block direct web access to non-public helper files
Options -Indexes

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
# Deny direct web access to helper/include files
Require all denied
